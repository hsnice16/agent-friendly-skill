{
  "name": "agent-friendly-skill",
  "version": "0.1.2",
  "private": true,
  "description": "Portable agent skill that scores the current repo's agent-friendliness on disk and recommends a model class.",
  "license": "MIT",
  "author": "Himanshu Singh (https://github.com/hsnice16)",
  "engines": {
    "node": ">=20.9.0"
  },
  "dependencies": {
    "ignore": "7.0.5"
  },
  "scripts": {
    "build": "ncc build src/index.ts -o skills/agent-friendly/dist --source-map --license licenses.txt",
    "format": "biome check --write",
    "lint": "biome check",
    "prepare-hooks": "lefthook install",
    "test": "node --test --import tsx tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.12",
    "@types/node": "20.19.39",
    "@vercel/ncc": "0.38.4",
    "lefthook": "2.1.6",
    "tsx": "4.21.0",
    "typescript": "5.9.3"
  }
}
